function [img warped] = warpImageMasked(img, mask, W)
% img: grayscale image
% mask: mask image (true at pixels that lie inside the template region
%       and are hence part of the tracking template andfalse everywhere
%       else
% W: warping matrix (W)
end

